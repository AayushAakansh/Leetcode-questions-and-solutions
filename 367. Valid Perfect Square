// Code

bool isPerfectSquare(long long int num) {

   if ( num == 1)
   {
    return true;
   }

   if ( num <0)
   {
    return false;
   }
   for ( long long int i = 0; i < num; i++)
   {
        if ( i*i == num)
        {
            return true;
        }
   } 
   return false;
}

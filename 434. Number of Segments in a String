int countSegments(char* s) {
    int l = strlen(s);
    char test = ' ';
    int count=0;
    for(int i=0; i<l; i++)
    {
        if (s[i] != ' ' && (i==0 || s[i-1] == ' '))
        {
            count++;
        }
    }
    return count;
}

bool isPalindrome(int x) {
    
    if (x<0)
    {
        return false;
    }
    long initial = x;
    long final = 0;
    while (x  != 0)
    {
        int temp = x % 10;
        final = final*10 + temp;
        x = x/10;
    }
    return final == initial;
}

//Code:

char repeatedCharacter(char* s) {
    int frequency[100]={0};
    for(int i=0; s[i] !='\0';i++)
    {
        int index=s[i]-'a';
        if (frequency[index] == 1)
        {
            return s[i];
        }
        frequency[index]++;
    }
    return 0;
}
